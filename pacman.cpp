// #include <fstream>
// using namespace std;
// int m;
// string line;
// int n = line.length();
// char arr[m][n];
// for(int j = 0; j< m; j++){
//     for (int i = 0; i < n; i++){
//         fin >> line;
//         arr[i][j] = line[i];
//     }
// }

#include <fstream>
using namespace std;

int main() {
    if()
}


if(getch(a)) {
    player_pos = x;
    player_pos = y - 1;
}
if(getch(d)) {
    player_pos = x;
    player_pos = y + 1;
}
if(getch(w)) {
    player_pos = x + 1;
    player_pos = y;
}
if(getch(s)) {
    player_pos = x - 1;
    player_pos = y;
}