#include <iostream>
using namespace std;

int main() {

    for (int a = 1; a < 6; a++){
        for (int b = 0; b < 6; b++){
            for (int c = 0; c < 6; c++){
                for (int d = 0; d < 6; d++){
                    for (int e = 0; e < 6; e++){
                        for (int f = 0; f < 6; f++){
                            if(a != b && a != c && a != d && a != e && a!= f){
                                if(b != c && b != d && b != e && b != f) {
                                    if (c != d && c != e && c!= f) {
                                        if (d != e && d != f) {
                                            if (e != f) {
                                                //cout << a << b << c << d << e << f << endl;
                                                // cout << a << b << endl;
                                                // cout << a << c << endl;
                                                // cout << a << d << endl;
                                                // cout << a << e << endl;
                                                // cout << a << f << endl;
                                                // //cout << b << a << endl;
                                                // cout << b << c << endl;
                                                // cout << b << d << endl;
                                                // cout << b << e << endl;
                                                // cout << b << f << endl;
                                                // cout << c << d << endl;
                                                // cout << c << e << endl;
                                                // cout << c << f << endl;
                                                // cout << d << e << endl;
                                                // cout << d << f << endl;
                                                // cout << e << f << endl;
                                                for (int i = 0; i ) {
                                                    cout << a                                                     
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}